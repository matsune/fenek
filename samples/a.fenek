fun get_ref_ref(a: i32**) {
  var b = a;
  **b = 21;
  ret;
}

fun get_ref(a: i32*) -> i32* {
  *a = 3;
  ret a;
}

fun get_val(a: i32) {
}

fun main() -> i32 {
  var a = 10;   
  *(&a) = 3;
  var b = &a; 
  *b = 11;    
  var c = b;  
  *c = 12;    
  var d = *c; 
  get_ref(b); 
  get_val(*b);
  get_ref(&d);
  get_val(d); 

  var e = &a;   
  var f = &e;   
  f = &b;       
  get_ref(*f);  
  get_ref_ref(f);

  ret a;
}
